# Opinion

Meeting: 0018-update-create-meeting-to-handle-existing-templates-and-add-tests
Agent: ArchitectB
Task: Update create_meeting to handle existing templates and add tests
Context:
- Some meeting artifacts exist from scaffolding runs with empty Task lines.
- Updating them must not disrupt any user-provided notes or context.
Position:
- A minimal, content-aware update is preferable to wholesale rewrites of summary/opinion files.
Recommendations:
- Keep the detection logic tightly focused on blank Task lines so other sections remain untouched.
- Confirm behavior with tests that mirror seeded templates and ensure repeated runs stay idempotent.
